from setuptools import setup, find_packages


setup(name='constantSorrow',
      version='0.0',
      description="I am the man of constant sorrow; I've seen special values as ints, all my days.",
      packages=find_packages())
